linter:
   hadolint:
     ignore:
       - DL3018

   misspell:
     locale: US

shellcheck:
    root_dir: project/
    target:
      - "**/*.{sh,bash}"
      - shebang: true
    include: [SC2104, SC2105]
    exclude:
    enable: all
    shell: bash
    severity: error
    norc: true
